services:
  php:
    container_name: kino_php_fpm
    build:
      context: docker/php8.4
      dockerfile: php-fpm-local.docker
    volumes:
      - ".:/var/www/app"
      - "./docker/php8.4/config/php.ini-${APP_ENV}:${PHP_INI_DIR:-default}/php.ini:ro"
      - "./docker/php8.4/config/pool.d/www.conf-${APP_ENV}:${PHP_FPM_DIR:-default}/www.conf:ro"
    working_dir: /var/www/app
    tty: true
    extra_hosts:
        - "apimgw.egov.uz:10.190.44.4"
        - "iskm.egov.uz:10.190.44.4"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - kino_network
  php-cli:
    container_name: kino_php_cli
    build:
      context: docker/php8.4
      dockerfile: php-cli-local.docker
    volumes:
      - ".:/var/www/app"
      - "./docker/php8.4/config/php-cli.ini-${APP_ENV}:${PHP_INI_DIR}/php.ini:ro"
    working_dir: /var/www/app
    restart: unless-stopped
    tty: true
    extra_hosts:
        - "apimgw.egov.uz:10.190.44.4"
        - "iskm.egov.uz:10.190.44.4"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - kino_network
  nginx:
    container_name: kino_nginx
    build:
      context: ./docker/nginx
      dockerfile: nginx.docker
    ports:
      - "127.0.0.1:${NGINX_LOCAL_PORT:-12002}:80"
    volumes:
      - "./:/var/www/app"
      - "./docker/nginx/config/nginx.conf:/etc/nginx/nginx.conf:ro"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    depends_on:
      - php
    networks:
      - kino_network
  db:
    container_name: kino_database
    image: postgres:16-alpine
    ports:
      - "${DB_LOCAL_PORT:-5436}:5432"
    volumes:
      - "kino_database:/var/lib/postgresql/data"
      - "./docker/postgres/postgresql.conf:/etc/postgresql/postgresql.conf"
      - "./docker/postgres/pg_hba.conf:/etc/postgresql/pg_hba.conf"
    environment:
      PGUSER: ${DB_USERNAME:-root}
      POSTGRES_DB: ${DB_DATABASE:-kino_database}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret!}
      POSTGRES_HOST_AUTH_METHOD: trust
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready", "-d", "${DB_DATABASE:-kino_database}" ]
      interval: 30s
      timeout: 60s
      retries: 5
      start_period: 80s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    networks:
      - kino_network
volumes:
  kino_database:
    external: true

networks:
  kino_network:
    external: true
